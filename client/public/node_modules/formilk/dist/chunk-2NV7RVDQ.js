// src/hooks/use-checkbox.tsx
import { jsx as _jsx } from "atomico/jsx-runtime";
import { useProp, useRef } from "atomico";
import { useRender } from "@atomico/hooks/use-render";
import { useFormListener } from "@atomico/hooks/use-form";
function useCheckbox(type) {
  const [name] = useProp("name");
  const [value] = useProp("value");
  const [checked, setChecked] = useProp("checked");
  const refInput = useRef();
  useFormListener("reset", () => setChecked(false));
  useRender(() => {
    return /* @__PURE__ */ _jsx("input", {
      type,
      name,
      value,
      ref: refInput,
      checked,
      onchange: ({ currentTarget: { checked: checked2 } }) => setChecked(checked2)
    });
  }, [checked, name, type]);
  return refInput;
}

export {
  useCheckbox
};
