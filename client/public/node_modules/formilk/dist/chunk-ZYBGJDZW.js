import {
  tokens_default
} from "./chunk-PXPI3F43.js";
import {
  custom_elements_default
} from "./chunk-HM7LLTML.js";

// src/scroll/scroll.tsx
import { jsx as _jsx } from "atomico/jsx-runtime";
import { c, css } from "atomico";
import { ScrollableComponentElement } from "scrollable-component";
function scroll({ width, height }) {
  return /* @__PURE__ */ _jsx("host", {
    shadowDom: true
  }, /* @__PURE__ */ _jsx("style", null, `:host{
            ${width ? `--width: ${width}` : ""}
            ${height ? `--height: ${height}` : ""}
        }`));
}
scroll.props = {
  width: {
    type: String
  },
  height: {
    type: String
  }
};
scroll.styles = [
  tokens_default,
  css`:host {
            width: var(--width, 100%);
            height: var(--height, 100%);
            --scrollbar-width: calc(var(--size) * 2);
        }`
];
var Scroll = c(scroll, ScrollableComponentElement);
custom_elements_default.define("scroll", Scroll);

export {
  Scroll
};
