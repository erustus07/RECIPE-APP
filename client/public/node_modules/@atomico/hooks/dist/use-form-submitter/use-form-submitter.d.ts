/**
 * Capture the submit of a form for submission
 * @param {import("atomico").Ref<HTMLFormElement>} ref
 * @param {Options} options
 * @returns {[any, import("../use-promise/use-promise").PromiseStatus,()=>void]}
 */
export function useFormSubmitter(ref: import("atomico").Ref<HTMLFormElement>, options: Options): [any, import("../use-promise/use-promise").PromiseStatus, () => void];
export { formToObject } from "@uppercod/form-tools";
export type Options = {
    action?: string;
    request?: RequestInit;
    formData?: (target: HTMLFormElement) => any;
    submit?: (data: any, options: {
        action: string;
        request: RequestInit;
    }) => any;
};
